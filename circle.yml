test:
 post:
   - find . -type f -regex ".*/target/surefire-reports/.*" -exec cp {} $CIRCLE_TEST_REPORTS/ \;
   - find . -type f -regex ".*/target/failsafe-reports/.*" -exec cp {} $CIRCLE_TEST_REPORTS/ \;
